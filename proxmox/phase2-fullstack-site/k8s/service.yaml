apiVersion: v1
kind: Service
metadata:
  name: phase2-fullstack-service
  labels:
    app: phase2-fullstack-app
    phase: phase2
    tier: backend
spec:
  type: NodePort
  selector:
    app: phase2-fullstack-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP